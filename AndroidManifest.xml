<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.projects.irledcontrol2"
    android:versionCode="2"
    android:versionName="2.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Black.NoTitleBar" >
        <activity
            android:name="com.projects.irledcontrol2.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.projects.irledcontrol2.Remote44KeyActivity"
            android:label="@string/title_activity_remote44_key" >
        </activity>
        <activity
            android:name="com.projects.irledcontrol2.Remote24KeysActivity"
            android:label="@string/title_activity_remote24_keys" >
        </activity>
    </application>

</manifest>
